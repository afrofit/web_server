import express from "express";

const router = express.Router();

// router.get("/:userId", getUserPerformanceData);
// router.post("/update/:userId", saveUserPerformance);

export { router as performanceRoutes };
